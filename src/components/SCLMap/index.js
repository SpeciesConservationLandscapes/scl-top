import React from 'react'
import LeafletMap from '../LeafletMap'
// import { useAuth0 } from '../../react-auth0-spa'

const SCLMap = () => {
  // const { token } = useAuth0()

  // console.log(token)
  return (
    <div>
      <LeafletMap />
    </div>
  )
}

export default SCLMap
